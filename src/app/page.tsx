// This is the page that will be rendered at the root of the app.
// It's not used for anything, but it needs to exist.
// The middleware will redirect the user to the correct locale.
export default function RootPage() {
  return null;
}
